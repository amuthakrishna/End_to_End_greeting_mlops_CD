apiVersion: serving.kserve.io/v1beta1
kind: InferenceService
metadata:
  name: indent-greeting
  namespace: ml-prod
spec:
  predictor:
    serviceAccountName: sa-s3-access
    sklearn:
      storageUri: REPLACE_MODEL_URI
